# 拉取镜像
docker pull yourusername/rss-bot-deps:latest

# 运行容器（挂载本地文件）
docker run -d \
  --name rss-bot \
  -v $(pwd)/rss.py:/app/rss.py \
  -v $(pwd)/.env:/app/.env \
  -v $(pwd)/data:/app/data \
  yourusername/rss-bot-deps:latest